//Check wheather the number is positive or negative or zero.

#include<stdio.h>
int main()
{
int num;
printf("Enter the number:");
scanf("%d",&num);

if(num>0)
{
printf("Number the positive.");
}
else if(num==0)
{
printf("Number the zero.");
}
else
{
printf("Number the negative.");
}
return 0;
}
